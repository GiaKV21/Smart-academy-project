export default function HTMLPage() {
  return (
    <div style={{ padding: "2rem" }}>
      <h1>HTML</h1>
      <p>
        HTML (HyperText Markup Language) is the foundation of every webpage,
        used to structure content and elements.
      </p>
    </div>
  );
}
