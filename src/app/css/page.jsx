export default function CSSPage() {
  return (
    <div style={{ padding: "2rem" }}>
      <h1>CSS</h1>
      <p>
        CSS (Cascading Style Sheets) controls the look and layout of HTML
        elements, from colors to spacing and animations.
      </p>
    </div>
  );
}
