export default function SQLPage() {
  return (
    <div style={{ padding: "2rem" }}>
      <h1>SQL</h1>
      <p>
        SQL (Structured Query Language) is used to manage and query data stored
        in relational databases.
      </p>
    </div>
  );
}
